/**
 * Layouts - Centralized exports for all layout components
 */

// App Layout - Used for authenticated app routes (/app/*)
export { default as AppLayout } from './AppLayout'

// Public Layout - Used for landing and auth pages (header with logo, nav links, no sidebar)
export { default as PublicLayout } from './PublicLayout'
